# qpm module (`/workspaces/mictlan/research/qpm/src/qpm/mgoslt/*.py`)
## Usage
```python
from qpm import mgoslt

mgoslt.sellmeier_n_eff(1.031, 70)
```
## Output
```
Array(2.1325023, dtype=float32)
```

# Task
For FW($1.064 \mu m$) and SHW($0.532 \mu m$):

1. Create `calc_index.py`
2. Perform tasks up to IndexConstruction in the document
3. Plot the refractive index distribution as a heatmap and output the HTML to out/
