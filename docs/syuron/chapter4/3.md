# 数値計算の速度と精度の評価

本手法の計算オーダーはUPAの解析解と同じくO(N)である。次に、実際に計算した結果を用いて速度と精度の評価を行う。

先行研究で述べられていた10mmのSHGの一定周期構造に続けてSFGの一定周期構造を追加したタンデム構造を考える。長さは第三高調波出力が得られる7.5mmとし、$\kappa_{\mathrm{SFG}}=2\kappa_{\mathrm{SHG}}$とし、励起光パワーを10Wとする。UPDとRK法(SciPy)と摂動法のそれぞれにおける第三高調波の振幅の分布をプロットと計算時間を以下に示す。

<!-- 図 -->

計算速度は概算でUPAの10倍程度、SciPyの1/60程度だった。ただし、UPAは励起光減衰を考慮できておらず、RK法も値が不正確である。なお、出力される第三高調波のパワーは約3W(振幅の2乗)と見積もられる。

次に精度を比較するために、デバイス長を長くとってManley-Roweの保存則からのずれを、SciPyで最も高精度なDOP853を用いた計算と併せて比較する。
